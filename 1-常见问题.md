# 常见问题与排查

## SRA 通用问题

### 和三月七小助手(March7Assistant/M7A)有什么区别？

SRA和三月七小助手(简称m7a/M7A)是两个不同的项目，由不同的开发者团队独立开发。

| 项目 | SRA v2.13 | m7a v2026.4.22 |
|---|---|---|
| 清体力 | ✅ | ✅ |
| 使用支援角色 | ✅ | ✅ |
| 启用培养目标 | ✅ | ✅ |
| 体力计划 | ✅ | ✅ |
| 每日实训 | ✅ | ✅ |
| 领取奖励 | ✅ | ✅ |
| 货币战争-标准博弈 | ✅ | ✅ |
| 货币战争-超频博弈 | ✅ | ✅ |
| 货币战争-刷开局 | ✅ | ❌ |
| 货币战争自定义阵容/羁绊 | ✅ | ❌ |
| 差分宇宙 | ⚠️ | ✅ |
| 模拟宇宙 | ❌ | ✅ |
| 锄大地 | ❌ | ✅ |
| 逐光捡金（深渊） | ❌ | ✅ |
| 自动对话 | ✅ | ✅ |
| 多账号托管 | ✅ | ❌ |
| 自定义任务 | ✅ | ❌ |
| 开发语言 | Python, C# | Python |
| Docker支持 | ❌ | ✅ |
| 云星穹铁道支持 | ⚠️ | ⚠️ |
| 下载大小 | 约120MB(zip) | 约700MB(zip) |
| 解压大小 | 约290MB | 约2GB |

### SRA能支持Linux/macOS/*nix吗？

云游戏已经支持Linux，但仅限于Edge浏览器。Linux支持还需要一段时间，macOS支持遥遥无期，其他系统不会考虑。

### 挂后台会抢鼠标吗？/可以挂后台吗？

包抢的。SRA 使用模拟操作技术，需要真实控制鼠标进行移动和点击，因此在挂后台时会占用鼠标。
如果你需要挂后台，可以通过 Windows 本地多用户、远程桌面、虚拟机等方式实现，也可以使用云游戏。

### 可以做一做绝区零的/有没有绝区零版本？

绝区零因抽不出角色导致退游，望周知，且市面上已有解决方案。

### 国际版是否可用？/Can the Global Edition of Honkai:Star Rail be used?

SRA已经支持国际服，在语言设置为简体中文的情况下可以使用。The globel edition is now supported and operates as expected with Simplified Chinese selected as the display language.

### 和maa是一个作者吗/和maa是什么关系？

SRA仅在命名和UI上部分借鉴MAA，除此之外与MAA并无联系。

### 会封号吗挺担心的

从SRA立项到2.14版本的更新，作者一直使用本人的账号进行测试，目前暂未封号。SRA依靠计算机图像识别和模拟操作运行，不会修改游戏文件或读写游戏内存。使用脚本或模拟操作是米哈游的封号理由之一，使用SRA需承担可能存在的风险。请不要在崩坏星穹铁道及米哈游在各平台的官方动态下讨论任何有关SRA的内容。

### 能用异种分辨率吗（例如1600×900）/只能使用1920*1080吗？

暂不支持异种分辨率，目前只能使用 1920 × 1080。

### Windows 8.1/8/7可以用吗？

Python 3.12 支持 Windows 8.1，但不支持 Windows 8 或更早版本（包括 Windows 7）。
SRA 中使用了 Python 3.12 的语法，不支持 Windows 8 或更早版本。

### 网页云崩铁可以嘛/适配云游戏吗？

2.13版本添加了云游戏适配。

### 下载速度太慢

SRA已接入Mirror酱，可以尝试使用。

### 会出手机版吗/手机平板能用吗？

不会，不同手机之间屏幕大小各不相同，实现难度较大。

### 为什么文件会被报告为具有可能有害的行为？

SRA请求了管理员权限，部分杀毒软件会误报。请将SRA目录添加入Windows Defender排除项以及防病毒软件的信任区。SRA不会对您的电脑造成任何损害。

## SRA启动问题

### Windows Defender阻止启动 | 恶意二进制信誉

**特征：** 控制台提示启动失败：An error occurred trying to start process "SRA-cli.exe" with working directory 'xxx'。应用程序控制策略已阻止文件。

**解决方法：** 打开Windows Defender，在左侧选择"应用与浏览器控制"，将"基于声誉的保护"关闭。

### 打开来是一个黑色窗口 | 找不到settings.json

特征：
- 打开后是一个 *黑色窗口*，里面有一堆带颜色的文字；
- 并且可能出现类似 `找不到 xxx\AppData\Roaming\SRA\settings.json` 的提示。

解决方法：
此问题通常出现在首次安装和启动 SRA 时。
请检查以下情况：
- 情况一：文件夹中同时存在 `SRA.exe` 和 `SRA-cli.exe`
  - 打开 `SRA.exe`，不要打开 `SRA-cli.exe`。
- 情况二：文件夹中只有 `SRA-cli.exe`
  - 你下载错版本了。如果下载的是 `StarRailAssistant_Core_版本号.zip` 或 `StarRailAssistant_Lite_v2.7.0.zip`，请删除后改下载
    `StarRailAssistant_版本号.zip` 或 `StarRailAssistant_版本号_Setup.exe`。

### 未安装.NET Runtime | You must install or update .NET to run this application.

特征：
- 打开 SRA.exe 提示 `You must install or update .NET to run this application.`

解决方法：
这是未安装 .NET Runtime 10 导致的。请安装 .NET Runtime 10 后重试。
点击 `Download it now`（或直接打开
https://dotnet.microsoft.com/en-us/download/dotnet/thank-you/runtime-10.0.7-windows-x64-installer
），在弹出的浏览器窗口中等待下载完成，然后运行安装程序。

### 无法启动配置：后端未运行 | 找不到后端可执行文件

**特征：** 控制台提示后端未运行，找不到xxx文件。

**解决方法：** 可能下错了版本，请下载完整版压缩包。也可能是后端文件被杀毒软件拦截删除，可将SRA的整个文件夹添加信任。

## 任务执行问题

### 刷本时无限滚动 | 找不到材料

**特征：** 刷本时进入材料选择界面后无限滚动。

**解决方法：** 确认材料是否按"命途"排序，而不是"更新时间"排序。

### 截图失败 | Error code from Windows: 0 - 操作成功完成

**特征：** 任务执行时控制台TRACE日志显示截图失败，游戏窗口任务栏图标闪烁。

**解决方法：** 手动点击游戏窗口以激活窗口。此问题是由于Windows认为当前正在使用SRA窗口，拒绝了SRA将游戏窗口置于前台的请求。

### 开始任务后立即结束

**特征：** 开始任务后没有任何行为，日志显示All tasks completed。

**解决方法：** 确认配置中是否勾选了任务和子任务。

### 配置好了任务但不能执行 | Invalid task ID

**特征：** 报错信息中存在Invalid task ID。

**解决方法：** 尝试重新添加任务。

### 差分宇宙不能执行

**特征：** 差分宇宙不能执行。

**解决方法：** 检查旷宇纷争大类是否勾选；检查开拓者名称是否填写；检查是否在差分宇宙界面执行；检查分辨率是否为1920*1080。

### 战斗时卡住/无法自动战斗

**特征：** 刷本或差分宇宙/货币战争时进入战斗后停住不动。

**解决方法：** 游戏设置中，其他→战斗功能→是否沿用自动战斗设置设为是，然后手动开启自动战斗。

### 图片识别失败

**特征：** 报错信息包含OCR识别结果为空或Could not locate the image。

**解决方法：**
- 多显示器用户：拔掉一个屏幕，或设置为镜像模式，或将SRA和游戏放在主屏幕上
- 检查游戏分辨率是否为1920*1080

### 找不到游戏

**特征：** 报错信息中存在No such file or directory。

**解决方法：** 在设置页面设置游戏路径，可开启自动检测或手动选择文件夹。

### 窗口未激活

**特征：** 任务执行时游戏窗口未被激活。

**解决方法：** 手动点击游戏窗口。
